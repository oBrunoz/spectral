// access curto: se vazar, a janela de estrago e pequena
export const ACCESS_TOKEN_TTL = '15m';

export const REFRESH_TOKEN_TTL_DAYS = 30;

export const REFRESH_COOKIE = 'boxd_refresh';

// o cookie so e enviado nas rotas de auth, nao em toda chamada da API
export const REFRESH_COOKIE_PATH = '/auth';
